<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.yuxs.springbootdev.modules.system.mapper.SysApiMapper">

    <!-- 根据用户ID三表联查该用户所拥有的所有可用 API 授权规则列表 -->
    <select id="selectApiPermissionsByUserId" resultType="java.lang.String">
        SELECT DISTINCT CONCAT(a.method, ':', a.path)
        FROM sys_api a
        INNER JOIN sys_role_api ra ON a.id = ra.api_id
        INNER JOIN sys_user_role ur ON ra.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND a.status = 0
    </select>

</mapper>
